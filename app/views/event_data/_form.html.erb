<%= form_for(@event_datum) do |f| %>
  <% if @event_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_datum.errors.count, "error") %> prohibited this event_datum from being saved:</h2>

      <ul>
      <% @event_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tracked_at %><br />
    <%= f.datetime_select :tracked_at %>
  </div>
  <div class="field">
    <%= f.label :event %><br />
    <%= f.text_field :event %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
